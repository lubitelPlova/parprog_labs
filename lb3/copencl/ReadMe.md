# How to setup

``` bash
mkdir build && cd build
cmake ..
make
```

# Usage

```
$ ./image_processor 
Usage: ./image_processor [INPUT_IMAGE_NAME] [OUTPUT_IMAGE_NAME]
Note that input and output must be jpg or png

```
